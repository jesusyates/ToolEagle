---
alwaysApply: true
description: 本会话首次实质性工作前 Read docs/MEMORY.md 一次；避免每条消息重复 Read 以省 token
---

# 记忆本 — 对齐策略（省 token · 硬性）

**适用范围**：本仓库（ToolEagle）内，助手对用户的回复。

## 你必须遵守

1. **`docs/MEMORY.md` 是唯一规则正文**（路径：工作区根目录相对路径 `docs/MEMORY.md`）。**不要**把全文复制进对话凑字数；**以磁盘文件为准**。

2. **同一对话线程内 — Read 频率（省 token）**  
   - **第一次**即将输出**实质性内容**（分析、方案、代码、命令、架构判断、产品决策、改路由/合规相关文件等）时，先 **`Read docs/MEMORY.md`** **一次**，再回复。  
   - **同一会话内后续用户消息**：**不要**每条都再 Read；在已读过且未触发下面「必须再读」条件时，沿用本会话已读内容并继续工作。  
   - **必须再 Read**（任一满足即执行）：用户明确要求 **「读记忆本」** / 对齐记忆本；你或用户**刚保存过**对 `docs/MEMORY.md` 的修改；即将做**重大**架构/合规/产品/国家站/CSE 决策且需要确认磁盘最新版；你**不确定**当前对话里记忆本是否仍与磁盘一致（例如线程极长、隔了很久）。  

3. **长对话**：若上下文已很长、或用户开始全新需求模块，可建议用户 **新开 Cursor 对话**，或你在关键节点 **再 Read 一次**（仍不必每条消息 Read）。

4. **例外**（可完全不 Read）：与项目无关的极短寒暄（如单独一句「谢谢」「好的」）。只要可能涉及本仓库，即不适用本例外。

5. **同一条用户消息**的多轮工具调用：若已在处理该消息时 Read 过 `docs/MEMORY.md`，本条消息内**不要**重复 Read。

6. **可见确认**  
   - 在**本会话中第一次**完成 Read 并对齐记忆本后的**第一条**实质性回复中，正文须明显包含 **「已读记忆规则」**（可注明「本会话首次」）。  
   - 若本轮按规则未 Read（纯寒暄、或本会话已读过且未触发再读），**不得**写「已读记忆规则」；可省略，或写 **「延续本会话，未重复读记忆本」**。

## 说明

这样可在**不删减** `MEMORY.md` 全文的前提下，减少**同一对话里反复注入全文**带来的 token 堆积。改记忆本只需保存 `docs/MEMORY.md`；触发「必须再读」条件后再 Read 即可对齐最新磁盘版。
