---
description: 上传 GitHub 前必须执行的全面检测流程
alwaysApply: true
---

# 上传 GitHub 前检测规则（必须遵守）

每次上传到 GitHub 之前，**必须**完成以下全面检测，确认正常后再执行 `git push`。

## 一、本地检测

1. **程序正常运行**
   - 执行 `npm run lint`，确保无 ESLint 错误
   - 执行 `npm run build`，确保构建成功
   - 如有测试：执行 `npm run test`，确保通过

2. **内容与数据一致性**
   - 检查即将发布的文章/页面数量
   - 确认 sitemap、robots 等配置与内容规模匹配
   - 避免出现：空页面、重复内容、异常数量突变

## 二、线上网站检测

1. **访问与功能**
   - 打开线上站点（tooleagle.com），确认可正常访问
   - 抽查关键页面：首页、核心工具页、新发布页面
   - 确认无 404、500 等异常

2. **搜索引擎可见性**
   - 确保 Google、百度等平台不会检测到异常
   - 异常包括：页面数量骤增/骤减、大量新页面突然出现、sitemap 与站点不一致
   - 如有大规模内容变更，需评估发布节奏，避免被判定为作弊

## 三、发布数量与节奏

- **必须搞清楚**：本次上传会新增/修改多少页面
- **原则**：发布数量、节奏需符合自然增长规律
- **禁止**：一次性推送大量异常内容，导致平台风控

## 四、执行顺序

```
1. npm run lint
2. npm run build
3. 检查本次变更的页面/文章数量
4. 访问线上站点，抽查关键页面
5. 确认无异常后，再执行 git add → commit → push
```

**未完成上述检测前，不得上传到 GitHub。**

## 五、完成后必须输出报告

每次全面检测和上传完成后，**必须**给出具体报告，便于用户和 ChatGPT 了解情况。

**报告格式**（以「## 上传检测报告（可转发 ChatGPT）」为标题）：

| 项目 | 结果 |
|------|------|
| Lint | 通过/失败 |
| Build | 通过/失败 |
| 本次变更 | 文件数、新增/修改页面数 |
| 线上抽查 | 访问正常/异常 |
| 推送 | 成功/失败，commit hash |

**附加说明**：遇到的问题、需关注的事项、建议的下一步。
