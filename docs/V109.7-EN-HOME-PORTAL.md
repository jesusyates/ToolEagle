# V109.7 — English Home as global portal (not tools hub)

**Constraints respected**: No layout/routes/metadata/sitemap changes; V109.4–109.6 nav/header paths unchanged; no removal of tool/guide routes or data sources.

## V109.7.1 (follow-up)

- **大海报首屏**：`HomeHeroGenerate` 使用 `min-h-[min(88vh,56rem)]`、全宽渐变 + 网格 + 背景 `Bird` 水印、**居中排版**与更大字号，首屏明确为「海报位」而非窄条文案区。
- **英文站首页去抖音/中文语境**：示例文案与 `startHereGuidesDesc`、Trending 引导语去掉平台堆砌；`TrendingMakeMoneySection` 在 `englishOnly` 下卡片标题优先 **`titleEn`**，副标为「How-to」；示例区去掉 `#fyp` 等强绑定单一 App 的标签。

---

## 1. Home role — before vs after

| Dimension | Before | After |
|-----------|--------|--------|
| **Primary story** | “Create viral content” + quick tool grids + platform directories | **Brand portal**: what ToolEagle is, who it’s for, where to go next |
| **Hero CTAs** | Primary pushed flagship generator + secondary “Browse tools” | **Explore tools** → `/tools` + **Browse guides** → `/en/how-to` (portal, not one-tool funnel) |
| **Tool aggregation** | Large “platform tools”, “popular tools”, `HomeQuickToolsSection` card grids on `/` | **Removed from home**; `/tools` remains the browse/search surface |
| **Douyin / CN** | No Douyin block on EN home in code reviewed | Still none in home body; CN discovery moved out of EN footer mega-column (see below) |
| **Discovery** | Blog strip + trending guides + many sections | **Trending EN guides** (`TrendingMakeMoneySection` `englishOnly`) kept as the single optional discovery strip; blog strip removed from home (still in nav/footer) |

## 2. New home module order

1. **`HomeHeroGenerate`** — Gradient “poster” band; tagline + H1 + subtitle; primary `/tools`, secondary `/en/how-to`; trust line only (no Pro upsell in hero).
2. **Why ToolEagle** — Three pillars (same content, updated `whyToolEagleDesc` for portal framing).
3. **`HomeStartHereSection`** — Three light cards: Tools, Guides, Pricing (links only).
4. **`ValueProofBlock`** — Social proof (unchanged variant `home`).
5. **`HomeExampleOutputs`** — Static samples; i18n labels via `home.examples*`.
6. **`TrendingMakeMoneySection`** (from `page.tsx`) — English how-to discovery.

**Removed from home composition**: `HomeQuickToolsSection`, platform tool matrix, popular `ToolCard` grid, latest blog cards, `CreatorModeDemo`.

**Kept as file (unused)**: `HomeQuickToolsSection.tsx` — can be repurposed or deleted later; not imported.

## 3. What was “tool-ified” on Home — now sunk to `/tools`

- **Browse by platform** + deep links to `/tiktok/...` style URLs — **removed from home**; still available under `/tools` (platform rows + categories).
- **Popular tools** grid — **home only removed**; same cards remain on `/tools` “Popular tools”.
- **Quick tools** multi-card section — **not rendered**; full catalog is `/tools`.

## 4. Home vs `/tools` copy boundary

| Page | Voice |
|------|--------|
| **Home (`messages/home`)** | Brand + orientation: global hub, who it’s for, “start here” rails, proof and samples. |
| **Tools (`messages/tools`)** | Action + selection: `subtitle` = “Pick a tool and start generating”; `intro` = browse/search and run a focused workflow. |

Implementation: `ToolsPageClient` uses `t("intro")` for the paragraph under the H1.

## 5. Footer (English `SiteFooter`)

**Fixed in this task**

- Replaced the **全中文栏目** (标题「中文」+ 多链接中文文案) with a **single English-labelled block**:
  - `footer.chinaSite`, `footer.chinaSiteBlurb`, `footer.chinaSiteLink` → one link to `/zh`.
- Bottom legal line already used **English** `Privacy` / `Terms` → `/privacy`, `/terms`.

**Subsequent / optional**

- Further column reorder or collapse on small screens.
- If Product Hunt / launch blocks should differ by region — product decision, not done here.

---

**Files touched**: `HomePageClient.tsx`, `HomeHeroGenerate.tsx`, `HomeStartHereSection.tsx` (new), `HomeExampleOutputs.tsx`, `ToolsPageClient.tsx`, `SiteFooter.tsx`, `messages/en.json`, `messages/zh.json`.

**Index**: Home assembly `src/app/_components/HomePageClient.tsx`; tools hub `src/app/tools/ToolsPageClient.tsx`.
