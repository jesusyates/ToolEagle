/** @deprecated Import from `@/hooks/useAuth` — kept for backward-compatible paths. */
export { AuthProvider, useAuth } from "@/hooks/useAuth";
export type { AuthContextValue, AuthSlice, AuthStatus, AuthUiIdentity } from "@/hooks/useAuth";
