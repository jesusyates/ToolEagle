export async function generateWithAI(prompt: string): Promise<string | null> {
  // Placeholder for future AI integration.
  // In Phase 4 this intentionally returns null so tools keep using
  // their existing rule-based generation logic.
  void prompt;
  return null;
}

