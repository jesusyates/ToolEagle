/** Global (primary) market — tone baseline; respects locale prefix when present. */

export function marketGlobalSystemSuffix(): string {
  return "Keep tone natural for the output language above; prioritize scroll-stopping clarity and actionable structure.";
}
