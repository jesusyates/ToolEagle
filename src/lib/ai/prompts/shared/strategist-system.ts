/** Global baseline system role — primary product voice (English-first strategist). */

export function sharedStrategistSystem(): string {
  return "You are a senior short-form video growth strategist. Always output valid JSON only.";
}
