/** Re-export shared auth policy (source: @tooleagle/auth-system). */
export {
  AUTH_MSG,
  AUTH_ERROR_CODES,
  AUTH_ACTIONS,
  OTP_RULES,
  mapAuthErrorToUserMessage,
  normalizeAuthError,
  type AuthMessageCode
} from "@tooleagle/auth-system";
